# 1. 本地同步版本
node scripts/sync-version.js 1.0.3

# 2. 提交推送
git add -A
git commit -m "bump: v1.0.3"
git push

# 3. 编译 Launcher（本地）
& "d:\Tu工作同步\My工作同步\openclaw_tools\scripts\build-local-launcher.ps1"

# 4. 服务器部署
ssh root@134.175.18.139
cd /opt/openclaw_tool_server
git pull
cd frontend && npm run build && cd ..
pm2 restart openclaw-backend

# 清理旧版本 Launcher
rm -f /opt/openclaw_tool_server/frontend/public/OpenClaw-Launcher-*.exe
rm -f /opt/openclaw_tool_server/frontend/dist/OpenClaw-Launcher-*.exe
rm -f /opt/openclaw_tool_server/frontend/dist_build/OpenClaw-Launcher-*.exe

# 5. 上传新 Launcher
scp "d:\Tu工作同步\My工作同步\openclaw_tools\frontend\public\OpenClaw-Launcher-v1.0.3.exe" root@134.175.18.139:/opt/openclaw_tool_server/frontend/public/